import { h } from "preact";
import { Playmate } from "../data/data";

export default function PlaymateTooltip({
  data,
  pinned,
}: {
  data: Playmate;
  pinned: boolean;
}) {
  return <div>eyoo</div>;
}
